services:
  - type: web
    name: visualization2-dashboard
    runtime: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
